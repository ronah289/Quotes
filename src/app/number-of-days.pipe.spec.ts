import { NumberOfDaysPipe } from './number-of-days.pipe';

describe('NumberOfDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberOfDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
